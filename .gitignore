# Environment / passwords
.env

# Node.js packages
node_modules/

# Logs
*.log

# Local tooling
.claude/

# Local migration source data
migration/*.csv
migration/*.txt

# Database backups (scripts/backup-database.ps1) — real student/payment
# data. This repo is public on GitHub; these must never be committed.
backups/

# Runtime state — regenerated automatically, meaningless on another machine
config/printer-lan-cache.json

# OS / Editor
.DS_Store
Thumbs.db
